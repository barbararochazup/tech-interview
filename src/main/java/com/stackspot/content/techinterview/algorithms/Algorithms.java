package com.stackspot.content.techinterview.algorithms;

public class Algorithms {

    /*
    You are going to be given a word. Your job is to return the middle character of the word.
    If the word's length is odd, return the middle character. If the word's length is even, return the middle 2 characters.

    #Examples:

    Algorithms.getMiddle("test") should return "es"

    Algorithms.getMiddle("testing") should return "t"

    Algorithms.getMiddle("middle") should return "dd"

    Algorithms.getMiddle("A") should return "A"

     */
    public static String getMiddle(String word) {
        return word;
    }


    /*
    Given two integers a and b, which can be positive or negative, find the sum of all the integers between and including them and return it.
    If the two numbers are equal return a or b.
    Note: a and b are not ordered!

    Examples (a, b) --> output (explanation)
        (1, 0) --> 1 (1 + 0 = 1)
        (1, 2) --> 3 (1 + 2 = 3)
        (0, 1) --> 1 (0 + 1 = 1)
        (1, 1) --> 1 (1 since both are same)
        (-1, 0) --> -1 (-1 + 0 = -1)
        (-1, 2) --> 2 (-1 + 0 + 1 + 2 = 2)

    Your function should only return a number, not the explanation about how you get that number.
     */
    public static int GetSum(int a, int b)
    {
        return 0;
    }
}
